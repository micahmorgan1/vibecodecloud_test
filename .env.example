# ── Required ──────────────────────────────────────────
DATABASE_URL=postgresql://user:password@host:5432/ats
JWT_SECRET=           # Generate: openssl rand -hex 32

# ── Server ────────────────────────────────────────────
NODE_ENV=production
PORT=3005

# ── CORS ──────────────────────────────────────────────
ALLOWED_ORIGINS=https://your-app.up.railway.app,https://your-domain.com

# ── Public URL (used in email links) ──────────────────
PUBLIC_URL=https://your-app.up.railway.app

# ── SMTP Email ────────────────────────────────────────
SMTP_HOST=smtp.postmarkapp.com
SMTP_PORT=587
SMTP_USER=your-postmark-server-token
SMTP_PASS=your-postmark-server-token
SMTP_FROM=WHLC ATS <noreply@whlcarchitecture.com>

# ── Seed User Passwords (optional, random if omitted) ─
# SEED_ADMIN_PASSWORD=
# SEED_MANAGER_PASSWORD=
# SEED_REVIEWER_PASSWORD=

# ── Optional Services ─────────────────────────────────
# CLAMAV_SOCKET=/var/run/clamav/clamd.ctl
# GOOGLE_SAFE_BROWSING_KEY=your-api-key
